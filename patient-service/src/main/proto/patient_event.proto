syntax = "proto3";

package patient.events;
option java_multiple_files = true; // we want to generate separate file for each class

message PatientEvent {
  string patientId = 1;
  string name = 2;
  string email = 3;
  string event_type = 4;
}

// compile edince target altında java patient.events içerisinde üretildi